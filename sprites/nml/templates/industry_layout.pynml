switch(FEAT_INDUSTRYTILES, SELF, ${industry.id}_${layout.id}, relative_pos) {
    <tal:build_industry_layout repeat="spritelayout layout.spritelayouts">
        relative_coord(${spritelayout[0]}, ${spritelayout[1]}): ${spritelayout[2]};
    </tal:build_industry_layout>
	${layout.default_tile};
}
