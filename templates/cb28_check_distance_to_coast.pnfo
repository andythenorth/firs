-1 * 15	 02 0A 10  //check distance to nearest coast
    85 
      8B 20 \wxFFFF // var, param (industry ID), shift, mask
      \2cmp 1A 00  \w16 // create a value (constant 1A)
      //7F DEBUG_PARAMETER 00 DEBUG_PARAMETER_INDUSTRY_TEXT_BIT // check if first bit is set for debug parameter
    01 //range
       04 80  \w02 \w02 //don't build industry if distance to coast exceeds limit
    10 00 //default      