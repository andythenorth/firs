# The local makefile should be ignored:
Makefile.local
firs*
sprites/firs.nfo
sprites/firs.pnfo
#The .renum dir
.renum/*.*

# Any built grfs, too:
./*.grf
./*.orig
./*.bak
./*log

# Ignore things the Mac likes to litter around the place:
.DS_Store